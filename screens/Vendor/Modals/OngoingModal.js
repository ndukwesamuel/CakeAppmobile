import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function OngoingModal({item}) {
  return (
    <View>
      <Text>OngoingModal</Text>
    </View>
  )
}

const styles = StyleSheet.create({})